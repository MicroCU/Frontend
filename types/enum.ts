export const enum Page {
  Journey = "journey",
  Recently = "recently",
  Search = "search"
}

export enum MicroType {
  VIDEO = "VIDEO",
  PRACTICE = "PRACTICE",
  TEST = "TEST"
}

export enum OnBoardMode {
  NOGOAL = "NOGOAL",
  GOAL = "GOAL"
}

export enum TabIconType {
  library = "Journey",
  search = "Search",
  history = "Recently"
}